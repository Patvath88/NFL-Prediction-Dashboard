# NFL Prop Predictor — Free Stats Edition

A Streamlit dashboard that uses free data (nfl_data_py) and live odds APIs to generate NFL prop predictions.

## Setup
```bash
pip install -r requirements.txt
export ODDS_API_KEY=your_odds_api_key
streamlit run app.py
```